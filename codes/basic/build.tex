\begin{lstlisting}
code tem.cpp
...
for i in {A..M}; do cp tem.cpp \$i.cpp; done;
code *.cpp
code run

寫入以下編譯與執行指令至 run 檔案中：
g++ -std=c++20 -DABS -Wall -Wextra -Wshadow $1.cpp -o $1 && ./$1

chmod +x run
\end{lstlisting}